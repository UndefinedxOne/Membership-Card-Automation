# ============================================
# Acuity → PassKit Bridge — Vercel Env Vars
# ============================================
# Add these in: Vercel Dashboard > Your Project > Settings > Environment Variables

# ---- Acuity Scheduling API Credentials ----
# Found at: Acuity > Integrations > API (scroll down)
ACUITY_USER_ID=your_acuity_user_id
ACUITY_API_KEY=your_acuity_api_key

# ---- PassKit REST API Credentials ----
# Found at: PassKit Dashboard > Developer Tools > REST Credentials
PASSKIT_API_KEY=your_passkit_api_key
PASSKIT_API_SECRET=your_passkit_api_secret

# PassKit API base URL
# Use https://api.pub1.passkit.io for default
# Use https://api.pub2.passkit.io for USA data instance
PASSKIT_API_URL=https://api.pub1.passkit.io

# ---- PassKit Membership Program Settings ----
PASSKIT_PROGRAM_ID=your_program_id
PASSKIT_TIER_ID=your_tier_id

# ---- Optional: Membership Product Filter ----
# Comma-separated, case-insensitive match against order title.
# Leave empty to process ALL orders.
MEMBERSHIP_PRODUCT_FILTER=
